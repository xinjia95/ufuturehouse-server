{% extends 'BackendBundle:Configuration:configuration.html.twig' %}

{% block pagecontent %}
    {{ form_start(form) }}
    <input type="submit" value="{{ 'save' | trans }}" class="btn btn-success" />
    {{ form_end(form) }}
{% endblock %}

