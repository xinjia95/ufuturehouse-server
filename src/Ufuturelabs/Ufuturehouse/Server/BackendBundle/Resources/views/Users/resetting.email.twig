{% block subject %}{{ 'users.resseting.subject' | trans({}, 'emails', default_locale) }}{% endblock %}

{% block body_text %}
    {% autoescape false %}
    {{ 'users.resseting.subject' | trans({
        '%name%':        user.name ~ ' ' ~ user.surname,
        '%link%':        confirmationUrl,
        '%companyName%': company.name
    }, 'emails', default_locale) }}
    {% endautoescape %}
{% endblock %}
